{
  "name": "audit-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Contractor audit scoring and compliance-trend analytics platform",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
